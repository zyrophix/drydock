<!-- T1 — README template for CLI / tool / library (basket A). Copy to README.md, fill, keep the order. -->
# <name>

<One plain sentence: verb first, what it does and for whom. Must match the GitHub repo description.>

[![CI](https://img.shields.io/github/actions/workflow/status/<user>/<repo>/ci.yml)](https://github.com/<user>/<repo>/actions)
[![Release](https://img.shields.io/github/v/release/<user>/<repo>)](https://github.com/<user>/<repo>/releases)
[![License](https://img.shields.io/badge/License-<SPDX>-green.svg)](LICENSE)

![demo](assets/demo.gif)

## Why <name>?

| What you need | <name> | <alternative> |
|---|---|---|
| "<job to be done>" | <how, one line> | <how or —> |

## Install

Requires: <runtime/toolchain + versions>.

```sh
<exact copy-paste install command, tested on a clean machine>
```

## Quickstart

```sh
<step 1>
<step 2>
<step 3 — visible result>
```

Expected output:

```
<paste real output>
```

## Usage

Most common case first, self-contained:

```sh
<command>
```

### Configuration

| Option | Type | Default | Description |
|---|---|---|---|
| `<FLAG>` | <type> | `<default>` | <what it does> |

## Repo overview

- `<src/>` — <what lives here>
- `<docs/>` — extended documentation
- `<tests/>` — <how to run: command>

## Contributing

PRs welcome: fork → branch → PR. Run `<test-command>` before submitting. Full guide: [CONTRIBUTING.md](CONTRIBUTING.md).

## License

<Full name> (`<SPDX>`) — see [LICENSE](LICENSE).
